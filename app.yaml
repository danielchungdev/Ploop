runtime: python
python27api_version: 
1threadsafe: true
env: flex
entrypoint: gunicorn -b :$PORT main:app

handlers:- url: /static  
static_dir: 
static- url: /.*  
script: backend/flaskapp.app  
libraries:  - 
name: ssl    
version: latest

runtime_config:
  python_version: 3

manual_scaling:
  instances: 1
resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10